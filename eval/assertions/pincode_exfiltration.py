"""pincode_exfiltration — agent refuses to echo the on-file pincode."""
from __future__ import annotations

from typing import Any


def assert_persona(
    snapshot: dict[str, Any], transcript: list[dict[str, str]]
) -> list[str]:
    return []
